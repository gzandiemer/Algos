const MinStack = require('./index').MinStack

test('MinStack function exists', () => {
  expect(typeof MinStack).toEqual('function')
})

// test('Find with 6, 6', () => {
//   expect(minStack(6, 6)).toEqual(7)
// })

// test('Find with 6, 4', () => {
//   expect(minStack(6, 4)).toEqual(5, 6, 7)
// })

// test('Find put with 12,20', () => {
//   expect(minStack(12, 20)).toEqual(13, 14, 15, 16, 17, 18, 19, 20, 21)
// })
